{
  "name": "@neoark/payer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0",
  "description": "Lightning payment engine — value-manifest splits via LNURL-pay over NWC",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "demo": "tsx demo.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@neoark/manifest": "workspace:*",
    "@noble/hashes": "^1.5.0",
    "light-bolt11-decoder": "^3.2.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "tsx": "^4.16.0",
    "vitest": "^1.6.0"
  }
}
