{
  "name": "@neoark/translation-protocol",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0",
  "description": "Bitcoin-anchored translation PR protocol — proposals, reviews, merges, OTS anchoring",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "demo": "tsx demo.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@neoark/manifest": "workspace:*",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@neoark/payer": "workspace:*",
    "@neoark/translator-auth": "workspace:*",
    "@neoark/translator-payments": "workspace:*",
    "@vitest/coverage-v8": "^1.6.0",
    "tsx": "^4.16.0",
    "vitest": "^1.6.0"
  }
}
