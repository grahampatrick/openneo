{
  "name": "@neoark/landing",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0",
  "description": "NeoArk landing page (neoark.org) — static, dark terminal aesthetic",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html",
    "serve": "node serve.mjs",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "vitest": "^1.6.0"
  }
}
